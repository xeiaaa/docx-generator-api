const title = require('./title')
const issuerInfo = require('./issuerInfo')
const emptyLine = require('./emptyLine')
const agreement = require('./agreement')
const agreementList = require('./agreementList')
const servicesTable = require('./servicesTable')
const optionalServicesTable = require('./optionalServicesTable')
const signatureBlock = require('./signatureBlock')

module.exports = {
  title,
  issuerInfo,
  emptyLine,
  agreement,
  agreementList,
  servicesTable,
  optionalServicesTable,
  signatureBlock,
}
